const palette = {
  red: 'red',
  blue: 'blue',
  pink: 'lightpink',
  lightBlue: 'lightblue',
};

const colors = {
  red: palette.red,
  pin: palette.pink,
  blue: palette.blue,
  lightBlue: palette.lightBlue,
};

export default {
  palette,
  colors,
  primary: colors.red,
  primaryInverse: colors.pink,
  secondary: colors.blue,
  secondaryInverse: colors.lightBlue,
};
